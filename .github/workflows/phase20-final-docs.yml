name: Phase 20 final evidence commits

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/phase20-final-docs.yml'

permissions:
  contents: write

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Configure commit identity
        run: |
          git config user.name "Sanskar"
          git config user.email "sanskarin@outlook.in"
      - name: Apply final evidence records
        run: python3 tools/phase20_final_docs.py
      - name: Remove temporary final evidence tools
        run: |
          git rm tools/phase20_final_docs.py .github/workflows/phase20-final-docs.yml
          git commit -m "chore: remove Phase 20 final evidence helper"
          git pull --rebase origin main
          git push origin HEAD:main
